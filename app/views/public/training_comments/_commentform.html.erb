<%= form_with model: [training, training_comment], local: false do |f| %>
  <%= f.text_area :comment, rows:'5', placeholder:"コメントを書く", class: 'w-100 comment-textarea' %>
  <%= f.submit "送信する" %>
<% end %>